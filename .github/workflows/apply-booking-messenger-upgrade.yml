name: Apply booking messenger upgrade
on:
  push:
    branches: [feature/booking-messenger-voice-chat]
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Apply patch
        run: node tools/apply-booking-messenger-upgrade.mjs
      - name: Commit generated changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "Implement booking-scoped encrypted messenger and voice signaling"
          git push origin HEAD:${GITHUB_REF_NAME}
